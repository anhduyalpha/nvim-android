-- plugins/init.lua — Plugin entry point
-- This file is loaded by lazy.nvim and imports all plugin specs
-- Returns empty table — actual specs are in sibling files (ui.lua, editor.lua, etc.)
-- lazy.nvim auto-discovers them via { import = "plugins" }

return {}
